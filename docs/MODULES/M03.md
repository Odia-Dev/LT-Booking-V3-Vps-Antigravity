# Module M03: User Profile & Roles

Defines database structures mapping customer roles and administrative roles, and configures basic profile editing endpoints.

---

# Status

✅ Complete

---

# Deliverables

- **APIs**: User Registration (`POST /api/auth/register`), Get Profile (`GET /api/profile`), Update Profile (`PUT /api/profile`).
- **Database changes**: Added `User` and `OtpVerification` schemas.
- **Frontend pages**: Customer login page (`/login`).
- **Admin pages**: User management links.

---

# Verification

- **Backend build**: PASS
- **Frontend build**: PASS
- **Prisma validate**: PASS
- **Database migration**: PASS
- **Manual testing**: Registered admin user, updated name/location details, verified edits persist in database.
